import React from 'react'
import styles from '@/app/h/[id]/client.module.scss'
import classNames from 'classnames/bind'

const cn = classNames.bind(styles)

const HonorClientPage = () => {
  return (
    <div>
      
    </div>
  )
}

export default HonorClientPage
